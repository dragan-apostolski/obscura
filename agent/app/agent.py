"""LangGraph agent.  (L04–L05 — placeholder until then.)

The agent becomes a StateGraph: retrieve → reason → (tool: spec-lookup / technique / compare) → answer.
For L01–L03 we answer without the graph; this file stays a stub.
"""
# TODO L04: build the StateGraph here.
