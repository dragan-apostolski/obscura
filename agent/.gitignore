.env
.DS_Store
.venv/
__pycache__/
*.pyc
data/*
!data/.gitkeep
.ragas_cache/
.pytest_cache/
.ruff_cache/
scratch/
# raw eval run dumps — regenerable, large; the .md reports are the kept record
evals/results/*.json

# local editor / MCP config (contains a project-specific Supabase ref)
.cursor/
.mcp.json

# product catalog (seed data, kept out of the public repo)
catalog/products.json
